// Re-exporta desde la ubicación canónica en src/config/ (bulletproof React structure).
// Este archivo se mantiene solo por compatibilidad hacia atrás.
export type { NavItem } from '#/config/nav-items'
export { NAV_ITEMS } from '#/config/nav-items'
